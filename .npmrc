# Electron tooling is happiest with a flat node_modules layout.
# pnpm still uses its global content-addressable store + hardlinks, so we keep
# the disk/speed wins while avoiding symlink-isolation issues with Electron and
# externalized native deps (mongodb, ssh2).
node-linker=hoisted
